---
layout: "_layouts/default.latte"
title: "Repertoár"
---

{block content}
<h1>REPERTOÁR</h1>
<p>
    Hrajeme rozmanitou škálu žánrů od popové hudby až k instrumentálním skladbám a to vše v moderním podání. Podívejte se také na naše videa na <a href="https://www.youtube.com/channel/UCOSQLzwqR1idmeTKp2BR1gA" target="_blank">YouTube</a>.
</p>
<hr>
<input type="text" id="repSearch" onkeyup="searchRepertoire()" placeholder="Hledat">
<table id="repertoire">
    <tbody>
        <tr>
            <th>Umělec</th>
            <th>Název skladby</th>
        </tr>
    {foreach $repertoire as $key => $action}
        <tr>
            <td>{$action['performer']}</td>
            <td>{$action['song']}</td>
        </tr>
    {/foreach}
    </tbody>
</table>

<script>
function searchRepertoire() {
  let input, filter, table, tr, i;
  let td = [];
  let txtValue = [];
  input = document.getElementById("repSearch");
  filter = input.value.toUpperCase();
  table = document.getElementById("repertoire");
  tr = table.getElementsByTagName("tr");
  for (i = 0; i < tr.length; i++) {
    td[0] = tr[i].getElementsByTagName("td")[0];
    td[1] = tr[i].getElementsByTagName("td")[1];
    if (td[0] || td[1]) {
      txtValue[0] = td[0].textContent || td[0].innerText;
      txtValue[1] = td[1].textContent || td[1].innerText;
      if (txtValue[0].toUpperCase().indexOf(filter) > -1 || txtValue[1].toUpperCase().indexOf(filter) > -1) {
        tr[i].style.display = "";
      } else {
        tr[i].style.display = "none";
      }
    }
  }
}
</script>

{/block}
